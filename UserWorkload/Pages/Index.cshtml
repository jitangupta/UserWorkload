@page
@model IndexModel
@{
    ViewData["Title"] = "Home";
}

<div id="db-loading" class="text-center mt-5">
    <div class="spinner-border text-primary" role="status"></div>
    <div class="mt-3">Waking up the database, please wait...</div>
</div>

<div id="main-content" style="display:none;">
    <div class="container mt-4">
        <div class="card mb-4">
            <div class="card-body">
                <h2 class="card-title">UserWorkload Project: Production-Ready User Management</h2>
                <p>
                    Built for startups who need professional user management without enterprise complexity or timeline. This demonstrates how business-smart cloud architecture delivers real functionality in days, not months.
                </p>
                <hr />
                <h3>Ready-to-Use Features:</h3>
                <ul>                    
                    <li> Secure user registration and authentication                                </li>
                    <li> Profile management with image uploads                                      </li>
                    <li> Clean, professional interface suitable for business use                    </li>
                    <li> Automated deployment pipeline (code to production in minutes)              </li>
                    <li> Built-in security: encrypted storage, hashed passwords, secure sessions    </li>
                </ul>
                <hr />
                <h3>Business-First Technical Choices:</h3>
                <ul>                   
                    <li>Azure Container Apps: Scales automatically without management overhead</li>
                    <li>SQL Database with cost optimization: Enterprise reliability at startup costs</li>
                    <li>Blob Storage: Secure, encrypted file handling</li>
                    <li>Key Vault: Professional secrets management</li>
                    <li>GitHub Actions: Automated, reliable deployments</li>
                </ul>
                <p>Total monthly cost: $15-25 (scales with usage)</p>
                <hr />
                <h3>Built for Growth:</h3>
                <p>This foundation can be extended with:</p>
                <ul>
                    <li>Role-based access control</li>
                    <li>SSO integration</li>
                    <li>Audit logging</li>
                    <li>API access</li>
                    <li>Custom business logic</li>
                </ul>
                <p>
                    Architecture supports these additions without rebuilding core functionality.
                </p>
                <hr />
                <h3>
                    Ideal for:
                </h3>
                <ul>
                    <li>Internal team portals and admin tools</li>
                    <li>Customer beta programs and partner access</li>
                    <li>Early-stage product user management</li>
                    <li>Proof-of-concept that needs to feel professional</li>
                    <li>Teams who want to focus on business logic, not authentication infrastructure</li>
                </ul>
            </div>
        </div>
    </div>
</div>

@section Scripts {
<script>
    async function checkDbStatus() {
        try {
            const res = await fetch('/api/dbstatus');
            const data = await res.json();
            if (data.status === "ready") {
                document.getElementById('db-loading').style.display = 'none';
                document.getElementById('main-content').style.display = '';
            } else {
                setTimeout(checkDbStatus, 2000);
            }
        } catch {
            setTimeout(checkDbStatus, 2000);
        }
    }
    checkDbStatus();
</script>
}
